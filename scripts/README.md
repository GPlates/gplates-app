This folder contains scripts to process and publish data to GPlates GeoServer.

The Python library [geoserver-pyadm](https://pypi.org/project/geoserver-pyadm/) is required for some of the scripts.

`pip3 install geoserver-pyadm`
